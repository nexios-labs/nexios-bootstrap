services:
  app:
    build: .
    ports:
      - "52789:52789"
    environment:
      - DATABASE_URL=postgres://postgres:password@postgres:5432/nexios
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=password
      - DB_NAME=nexios
    depends_on:
      - postgres
    volumes:
      - ./src:/app/src
      - ./logs:/app/logs
    networks:
      - nexios-network

  postgres:
    image: postgres:15
    environment:
      - POSTGRES_DB=nexios
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - nexios-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 2s
      timeout: 2s
      retries: 10

  # Redis service (commented out as requested)
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data
  #   networks:
  #     - nexios-network

volumes:
  postgres_data:
  # redis_data:

networks:
  nexios-network:
    driver: bridge
